function arraySum(arr) {return arr.reduce((acc, val) => acc + val, 0);}function arrayAverage(arr) {return arraySum(arr) / arr.length;}function arrayMax(arr) {return Math.max(...arr);}function arrayMin(arr) {return Math.min(...arr);}function arrayUnique(arr) {return [...new Set(arr)];}function arraySort(arr) {return arr.slice().sort((a, b) => a - b);}function arrayReverse(arr) {return arr.slice().reverse();}function arrayFilter(arr, callback) {return arr.filter(callback);}function arrayMap(arr, callback) {return arr.map(callback);}function arrayFind(arr, callback) {return arr.find(callback);}function arrayIncludes(arr, value) {return arr.includes(value);}function arrayJoin(arr, separator) {return arr.join(separator);}function arraySlice(arr, start, end) {return arr.slice(start, end);}function arraySplice(arr, start, deleteCount, ...items) {arr.splice(start, deleteCount, ...items);return arr;}function arrayConcat(arr1, arr2) {return arr1.concat(arr2);}function arrayIndexOf(arr, value) {return arr.indexOf(value);}function arrayLastIndexOf(arr, value) {return arr.lastIndexOf(value);}function arrayEvery(arr, callback) {return arr.every(callback);}function arraySome(arr, callback) {return arr.some(callback);}function arrayFill(arr, value, start = 0, end = arr.length) {return arr.fill(value, start, end);}function arrayFlat(arr, depth = 1) {return arr.flat(depth);}function arrayFlatMap(arr, callback) {return arr.flatMap(callback);}function arraySortDescending(arr) {return arr.slice().sort((a, b) => b - a);}function arrayChunk(arr, size) {const result = [];for (let i = 0; i < arr.length; i += size) {result.push(arr.slice(i, i + size));}return result;}function arrayTranspose(matrix) {return matrix[0].map((_, colIndex) => matrix.map(row => row[colIndex]));}function arrayZip(...arrays) {return arrays[0].map((_, i) => arrays.map(array => array[i]));}function arrayRemoveDuplicates(arr) {return arr.filter((item, index) => arr.indexOf(item) === index);}function arrayShuffle(arr) {for (let i = arr.length - 1; i > 0; i--) {const j = Math.floor(Math.random() * (i + 1));[arr[i], arr[j]] = [arr[j], arr[i]];}return arr;}function arrayRandomElement(arr) {return arr[Math.floor(Math.random() * arr.length)];}function arrayCountOccurrences(arr) {return arr.reduce((acc, val) => {acc[val] = (acc[val] || 0) + 1;return acc;}, {});}
